# Entorno para el notebook diffusion-model/obj-1/re_1-1/re_1-1.ipynb
# Verificado contra los imports de todas las celdas.
# CPU-only por defecto (adecuado para el smoke test local en Windows sin GPU).
# En Colab / Kaggle no se necesita este archivo, esos entornos ya traen torch con CUDA.

# Numericos y datos
numpy>=1.24,<2.0
pandas>=2.0,<2.3
scikit-learn>=1.3,<1.6

# Deep learning (CPU wheels en Windows por defecto)
torch==2.3.1
torchvision==0.18.1

# Stack de difusion
diffusers==0.27.2
transformers==4.40.2
accelerate==0.30.1
safetensors==0.4.3
huggingface_hub>=0.23,<0.25

# Imagen
Pillow>=10.0,<11.0

# Plotting
matplotlib>=3.7,<3.10

# Ejecucion de notebooks
jupyter>=1.0
ipykernel>=6.29
notebook>=7.0
